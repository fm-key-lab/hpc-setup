version: '3'

includes:
  envs: ./setup-tasks/envs/EnvsTasks.yml
  install: ./setup-tasks/InstallTasks.yml
  download: ./setup-tasks/PublicDataTasks.yml

tasks:
  setup:
    prompt: Are you setting up a new HPC?
    cmds:
      - task: install:core
      - task: download:all