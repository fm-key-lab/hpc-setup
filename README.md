# HPC Setup

A collection of [Taskfiles](https://taskfile.dev) for setting up and managing lab HPCs

## Usage

```bash
cd $GROUP_HOME/tools/hpc-setup
module load task
task --dry
# task: [default] install:core
```

## Troubleshooting

- Ensure that the `$GROUP_HOME` environment variable is set